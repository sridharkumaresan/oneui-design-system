---
"@functions-oneui/tokens": minor
"@functions-oneui/theme": minor
"@functions-oneui/organism-hero-banner": minor
---

Add a shared semantic surface system for branded backgrounds and reusable surfaces.

The update adds structured raw solid surface primitives in `@functions-oneui/tokens`, semantic surface recipes and registry/policy/resolver helpers in `@functions-oneui/theme`, and migrates `HeroBanner` to consume shared semantic surface keys while preserving compatibility for legacy gradient-based inputs.
