# organisms

Container directory for future individually published organism packages.

Organisms are intentionally deferred in the Windows-first phase-1 rebuild. Add them back only after the base workspace passes the same install, test, and Storybook flows on macOS and native Windows.
