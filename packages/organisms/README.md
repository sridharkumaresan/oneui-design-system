# organisms

Individually published organism packages for OneUI.

Each organism under this directory is published independently as:

- `@functions-oneui/organism-<name>`

Current organisms compose atoms and theme primitives without embedding application business logic. Keep new organisms UI-only, accessibility-first, and importable from their package root only.
